import XCTest
@testable import NetworkAvailabilityMonitor

final class NetworkAvailabilityMonitorTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(NetworkAvailabilityMonitor().text, "Hello, World!")
    }
}
